# Solana RPC URL
# Set this to use a custom Solana RPC endpoint (e.g., Helius, QuickNode, or your own node)
# If not set, defaults to https://api.devnet.solana.com
# This variable is exposed to the client-side and will be available in production builds
# Example values:
#   - Devnet: https://api.devnet.solana.com
#   - Testnet: https://api.testnet.solana.com
#   - Mainnet: https://api.mainnet-beta.solana.com
#   - Custom RPC: https://your-rpc-endpoint.com
NEXT_PUBLIC_SOLANA_RPC_URL=

# ─── SIX Financial Data API ───────────────────────────────────────────────────
# Real-time FX rates and precious metal spot prices for the Treasury Vault.
# Sign up / get credentials via the StableHacks hackathon portal:
#   https://dorahacks.io/hackathon/stablehacks/detailform
#
# These variables are SERVER-SIDE ONLY (no NEXT_PUBLIC_ prefix).
# The app proxies requests through /api/six-rates to keep credentials secure.
#
# Base URL (default points to SIX Web API findata v1):
SIX_API_BASE_URL=https://api.six-group.com/api/findata/v1
#
# Authentication — use ONE of the following:
#
# Option A: Bearer token (preferred)
SIX_API_TOKEN=
#
# Option B: Basic auth
SIX_API_USERNAME=
SIX_API_PASSWORD=

# ─── Solstice Finance USX / YieldVault API ────────────────────────────────────
# Live instruction API for minting USX and locking into YieldVault (~11.5% APY).
# Provided via the StableHacks hackathon portal (Solstice Finance, March 2026).
# Docs: https://instructions.solstice.finance/docs/
# Full docs: https://docs.solstice.finance/solstice-for-builders/apis
#
# These variables are SERVER-SIDE ONLY.
# The app routes requests through /api/usx-instructions to keep the key secure.
#
USX_API_KEY=
#
# Override the base URL if needed (defaults to instructions.solstice.finance):
# USX_API_URL=https://instructions.solstice.finance
